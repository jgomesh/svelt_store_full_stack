const config = {
    username: 'postgres',
    password: '123456',
    database: 'tsauth',
    host: 'localhost',
    port: 5432,
    dialect: 'postgres',
    logging: false,
};

module.exports = config;
